{
  "name": "regexmap",
  "version": "1.1.4",
  "description": "Map multiple regular expressions to one or more strings",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha",
    "coverage": "node node_modules/.bin/istanbul cover _mocha -- -R spec",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "keywords": [
    "regular",
    "expressions",
    "regex",
    "regexp",
    "map",
    "mapping",
    "regexes",
    "string",
    "match"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/therewillbecode/regexmap"
  },
  "author": "therewillbecode <tomw08@gmail.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/therewillbecode/regexpmap/issues"
  },
  "homepage": "https://github.com/therewillbecode/regexmap",
  "dependencies": {
    "lodash": "^4.15.0",
    "rewire": "^2.5.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.11.14",
    "istanbul": "^0.4.5",
    "mocha": "^3.0.2"
  }
}
